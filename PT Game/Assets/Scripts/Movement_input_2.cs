using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Movement_input_2 : MonoBehaviour
{
    public float speed;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //float l = Input.GetKeyDown("a");
       // float u = Input.GetKeyDown("w");
        //float r = Input.GetKeyDown("d");
       // float d = Input.GetKeyDown("s");

       // gameObject.transform.position = new Vector3(transform.position.x + (l * speed),
        //   transform.position.y + (u * speed), -6.35f);
    }
}
